{
  "name": "rtls-swarm-tool",
  "version": "0.0.1",
  "description": "Swarm management tool for RTLS-Link devices",
  "scripts": {
    "dev": "concurrently \"npm run --prefix server dev\" \"npm run --prefix ui dev\"",
    "build": "npm run --prefix server build && npm run --prefix ui build",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1",
    "concurrently": "^8.2.2",
    "@types/node": "^20.11.5"
  },
  "workspaces": [
    "server",
    "ui"
  ]
}
